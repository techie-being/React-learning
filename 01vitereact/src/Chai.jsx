function Chai1(){
    return(
        <h1>jai shree mahaprabhu </h1>
    )
       
}
export default Chai1

//this function chai is a called components in react. 
//it is a good habbit to declare a function/components name first letter should be capital.
// even if we declare our component file name in capital it is better.


/* ⚡ Inner Workflow Summary till now

Browser loads index.html → finds #root.

Vite runs main.jsx → mounts React app inside #root.

App.jsx is rendered → acts as the main layout.

Inside App, <Chai /> is called → inserts Chai’s <h1>.

Final UI is injected into the DOM.   */

